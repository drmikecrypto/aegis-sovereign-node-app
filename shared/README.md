# Shared Assets

- `binaries.json` – authoritative manifest of verified Sonic node binaries.
- `scripts/` – tooling for checksum verification, signature validation, and downloads (to be added).

All companion projects read from this manifest to ensure identical binary provenance.

